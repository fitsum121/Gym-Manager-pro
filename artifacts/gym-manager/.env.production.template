# Production Environment Variables for Gym Manager Mobile App
# Copy this file to .env before running `eas build --profile production`
# NEVER commit the filled-in .env file to git.

# Required — point to your deployed API server URL (no trailing slash)
EXPO_PUBLIC_API_URL=https://your-api-server.onrender.com
